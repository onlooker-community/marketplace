# Development 

## Installation

Install this directory:

```bash
git clone git@github.com:onlooker-community/marketplace
```

We use mise to manage our runtime deps

```bash
mise install
```

Then run `bun install` to install our development deps (for linting, formatting)
